
services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8080
    volumes:
      - .:/app
      - ./data:/app/data
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - DEBUG=1
